# LD Customer Organization & IAM Boundary Engine v1

Status: DEVELOPMENT / NON-PRODUCTION

Purpose: define tenant, organization, role and authorization boundaries behind the existing Client Portal.

This is not a second portal UI. It is the policy layer that prevents one customer organization from accessing another customer's projects, commercial documents, support records or entitlements.

Default posture: deny unless explicitly allowed.
