`vrs-supply-chain-oidc` accepts only GitHub OIDC tokens issued for repository `lundus88/fieldgis-reference`, ref `refs/heads/main`, audience `vrs-supply-chain-attestation`, and workflow `.github/workflows/vl-supply-chain-attestation.yml`. It uses the Supabase service role internally to call the protected gate-recording RPC. No production promotion or human approval action is exposed by this function.
